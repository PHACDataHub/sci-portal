app:
  title: Data Science Portal
  baseUrl: http://localhost:3000

organization:
  name: PHAC

backend:
  # Used for enabling authentication, secret is shared by all backend plugins
  # See https://backstage.io/docs/auth/service-to-service-auth for
  # information on the format
  # auth:
  #   keys:
  #     - secret: ${BACKEND_SECRET}
  baseUrl: http://localhost:7007
  listen:
    port: 7007
    # Uncomment the following host directive to bind to specific interfaces
    # host: 127.0.0.1
  csp:
    connect-src: ["'self'", 'http:', 'https:']
    # Content-Security-Policy directives follow the Helmet format: https://helmetjs.github.io/#reference
    # Default Helmet Content-Security-Policy values can be removed by setting the key to false
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  # This is for local development only, it is not recommended to use this in production
  # The production database configuration is stored in app-config.production.yaml
  database:
    client: better-sqlite3
    connection: ':memory:'
  # workingDirectory: /tmp # Use this to configure a working directory for the scaffolder, defaults to the OS temp-dir

  # Configuration for custom plugins
  plugins:
    provisioner:
      repo:
        owner: ${GITOPS_REPO_OWNER}
        name: ${GITOPS_REPO_NAME}

integrations:
  github:
    ### Use a GitHub App for authentication. See https://backstage.io/docs/integrations/github/github-apps.
    - host: github.com
      apps:
        - $include: github-app-integration-credentials.yaml

    ### Example for how to use a Personal Access Token or PAT from GitHub. You can find out how to generate this token,
    #### and more information about setting up the GitHub integration here: https://backstage.io/docs/integrations/github/locations#configuration
    # - host: github.com
    #   token: ${GITHUB_TOKEN}

    ### Example for how to add your GitHub Enterprise instance using the API:
    # - host: ghe.example.net
    #   apiBaseUrl: https://ghe.example.net/api/v3
    #   token: ${GHE_TOKEN}

proxy:
  ### Example for how to add a proxy endpoint for the frontend.
  ### A typical reason to do this is to handle HTTPS and CORS for internal services.
  # endpoints:
  #   '/test':
  #     target: 'https://example.com'
  #     changeOrigin: true

# Reference documentation http://backstage.io/docs/features/techdocs/configuration
# Note: After experimenting with basic setup, use CI/CD to generate docs
# and an external cloud storage when deploying TechDocs for production use-case.
# https://backstage.io/docs/features/techdocs/how-to-guides#how-to-migrate-from-techdocs-basic-to-recommended-deployment-approach
techdocs:
  builder: 'local' # Alternatives - 'external'
  generator:
    runIn: 'docker' # Alternatives - 'local'
  publisher:
    type: 'local' # Alternatives - 'googleGcs' or 'awsS3'. Read documentation for using alternatives.

# See https://backstage.io/docs/auth/ to learn about auth providers
auth:
  environment: development
  providers:
    # See https://backstage.io/docs/auth/github/provider
    github:
      development:
        clientId: ${AUTH_GITHUB_CLIENT_ID}
        clientSecret: ${AUTH_GITHUB_CLIENT_SECRET}

        # Authentication will fail without at least one resolver
        signIn:
          resolvers:
            - resolver: usernameMatchingUserEntityName

    # See https://backstage.io/docs/auth/guest/provider
    guest: {}

scaffolder:
  # see https://backstage.io/docs/features/software-templates/configuration for software template options

costInsights:
  engineerCost: 200000
  products:
    projectBudgets:
      name: Project Budgets
      icon:
    costCentres:
      name: Cost Centres
  baseCurrency:
    locale: en-US
    options:
      currency: CAD
      minimumFractionDigits: 3

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location]
  locations:
    # Backstage entities
    - type: file
      target: ../../templates/entities.yaml

    # Resource Provisioner template
    - type: file
      target: ../../templates/resource-provisioner.yaml
      rules:
        - allow: [Template]

    # GCP Project Provisioner template
    - type: file
      target: ../../templates/gcp-project-provisioner.yaml
      rules:
        - allow: [Template]

    # Backstage organizational data
    - type: file
      target: ../../templates/org.yaml
      rules:
        - allow: [User, Group]
