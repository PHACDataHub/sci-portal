apiVersion: tf.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: t-project-003 # "TO BE PATCHED" (with project ID)
spec:
  configuration: |
    terraform {
      backend "gcs" {
        bucket  = "433b88e1164c5268-bucket-tfstate"
        prefix  = "by-project-id/t-project-003"
      }
    }

# TODOs
# - [x] Make a bucket at the SciencePlatform level
# - Get the Project ID
        # prefix  = "by-project-id/${var.project_id}/state"
# - Decide on how to configure the bucket name
# - Remove the global ProviderConfig.
# - Document that we're expecting the providers to be configured
